# B12｜产品流程与验收

版本：V2｜本文件是产品或服务生产流程设计，未实现、未测试。

## 1. 客户看到的页面或材料

1. 今日待出待回任务。
2. 器材扫码页。
3. 出入库核对页。
4. 待检和损坏队列。
5. 订单时间占用页。
6. 异常冲突页。
7. 库存差异和导出页。每次操作给明确成功或未提交反馈，弱网场景能看到哪一笔需要补录。

## 2. 最小数据与追溯关系

器材ID、编号、类别、位置、实物状态；订单ID、客户代号、起止时间、缓冲时间、器材明细；流转事件ID、操作者、提交时间、交接数量、照片；检查记录和恢复人；状态更正原因。时间统一采用半开区间[start,end)，清洁缓冲占用额外时段并显示。

所有对象都有固定编号和版本关系；变更能追到谁在何时改了哪项。真实客户数据不默认放入本研究仓库；方案里的例子使用虚构材料，实际试点另设受控位置并在记录中引用。

## 3. 不能省略的业务规则

1. 可租判断分为“当前实物可发”和“未来时间可预约”，未来预约的归还假设要显式展示。
2. 逾期归还不能自动恢复可用。
3. 预订和冲突检查一起提交。
4. 取消订单只释放该订单占用，不改变损坏状态。
5. 重复扫码需识别同一事件。
6. 首版只支持序列号件，数量件另开验收后才开放。

## 4. 场景验收表

以下为未来执行的测试设计。结果当前全部为“未执行”，填写时必须附版本、输入和实际结果，不得仅凭演示通过。

| 编号 | 场景与预期 | 实际结果 |
| --- | --- | --- |
| B12-A01 | 同时抢同一时段同一件：只能一单成功 | 未执行 |
| B12-A02 | A延期到B开始后：标出B冲突，不静默重分配 | 未执行 |
| B12-A03 | 归还但待清洁：不可发货 | 未执行 |
| B12-A04 | 普通配送员恢复损坏件：拒绝 | 未执行 |
| B12-A05 | 扫码重试：不重复流转 | 未执行 |
| B12-A06 | 取消未来订单：释放预约但保留历史 | 未执行 |
| B12-A07 | 断网出库：明确未提交，回网人工核对。 | 未执行 |

## 5. 交付前后的质量动作

先以虚构样本核验业务规则，再用授权脱敏输入对照旧流程，最后才进行限定范围真实试点。每个用例记录输入版本、操作人、预期、实际、证据位置和问题编号。规则通过不代表客户采用，客户自主完成和付款另看试点文件。

关键结果、原始资料、权限或恢复存在错误时，不让客户继续依赖受影响结果。定位到具体版本和范围，给可靠临时处理方式，修复后重跑相关用例及核心完整流程；保留原问题记录，不把失败覆盖成通过。

## 6. 首版变更控制

实施方案的P0范围为当前基线。新需求登记提出角色、发生频次、替代办法、增加成本、客户是否愿付费；只在真实交付反复遇到后排P1。第二个客户要求不同业务规则时先判断是否仍属同类客户，不能通过无限配置掩盖定制服务。

每次交付带版本说明；服务成果保留修订差异，软件更新保留数据备份和可用的恢复路径。导出、迁移和恢复的验收范围与产品承诺一致，不因文件生成成功就认定内容完整。

[项目目录](README.md) · [试点排期](04-试点排期与经营复盘.md)
