# B16 印刷校样版本确认 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在本idea的coding目录实现有真实身份、PostgreSQL状态和文件存储的建单→上传校样→发送确认→客户确认／退回→新发版→当前版印前复核→回执导出闭环。

**Architecture:** Vue网页通过同域Django/DRF API操作本idea独立PostgreSQL；事务内写业务事件和任务表，worker生成导出。原件私有存储，下载每次服务端鉴权，客户端不决定权限或业务状态。

**Tech Stack:** Vue 3、TypeScript、Vite、Python、Django 5.2 LTS、DRF、PostgreSQL、阿里云OSS/ECS/RDS；PDF.js与WeasyPrint；pytest-django、Playwright。

**Spec:** [采用技术基线V2](../07-方案与详细设计/08-技术选型与架构方案.md)、[用户需求V3](../06-产品需求与版本规划/03-产品需求说明书.md)、[状态权限](../07-方案与详细设计/04-数据状态与权限设计.md)。执行者须同时读取三份文件。

## Global Constraints

- **计划待用户审阅，产品尚未编码。** 用户已指定默认子代理执行，审阅通过后保留该方式；本文件不安装依赖、不创建源码、不假称测试运行。
- 原采用基线原文：“Vue 3＋TypeScript＋Vite、Python Django 5.2 LTS＋DRF、PostgreSQL、数据库任务表／Outbox；阿里云中国内地 ECS＋RDS PostgreSQL＋私有 OSS，按 idea 独立部署与数据库隔离”。
- 原试点边界：“两店以内、合计10个低风险订单；容量验证按每店50单／月、2GB，超额仍可读取和导出既有历史。”
- 原权限原则：“所有表按本项目客户组织携带 tenant_id；子对象查询从已授权父对象范围开始。”
- 原恢复目标：“建议试点内部目标 RPO≤24小时、RTO≤4小时，只有计时恢复演练后才能转为承诺。”
- 本idea源代码只放`coding/backend`、`coding/frontend`，部署放`coding/deploy`。本项目是手机兼容网页，不创建伪原生`app`壳；原生移动项目才使用`coding/app`。
- PostgreSQL为开发、事务测试及生产数据库；禁止用SQLite替代并发验收。正式业务库为空，浏览器原型假数据、角色切换及sessionStorage不能自动迁入产品。
- 本轮编码范围包含国内10个项目；B16先行验证可复用工程约定，其余项目按依赖分批并行完成编码，不再等待获客才开发。各idea不共享业务库；同一项目事务模块不由多代理同时编辑。
- 未配置真实OSS／验证码／部署账号时，本地功能可验收但对应真实集成标“未执行”；不可自动使用成功桩冒充外部服务已接通。自动付费采购、对外消息发送及生产发布不在此计划执行范围。

## Review Focus

- 旧版页面停留后点击确认：409并引导新请求（Task 4）。
- 撤销客户入口后重发Range下载：403，不发OSS直链（Task 3）。
- 两名员工同时发送不同版本：只一版本成为当前，冲突版本保留（Task 4）。
- 全角中文文件名、重复文件名、路径字符：安全显示并按文件ID区分（Task 3）。
- 创建第二单再返回首单：意见、草稿和回执独立（Task 6）。

---

## 文件边界与运行约定

所有下述路径以本idea目录为根。`coding/backend/config`只管配置路由；`identity`只管会话、组织与授权；`proofing`封装本业务状态与事务；`files`管隔离上传与私有下载；`jobs`管任务租约；`exports`管不可变导出；`frontend/src/features/proofing`放页面和交互。接口错误统一为`{"code": string, "message": string, "current_revision": number|null}`，未登录401、越权403、不存在404、修订／状态冲突409、字段422，附件超限413。

所有命令携带`Idempotency-Key`和`expected_revision`；键作用域是tenant＋actor＋操作＋对象，保存规范化请求摘要。同键同体返回原结果，同键异体409。列表分页仅列当前tenant授权对象，传tenant_id不能改变身份范围。领域核心：Order(title, recipient_contact, revision, current_version_id)；ProofVersion(order_id, sequence, asset_id, sha256)；ConfirmationRequest(version_id, recipient_id, expires_at, revoked_at)；ConfirmationEvent(request_id, decision, reason, actor_id)；PrintReview(version_id, actor_id)。约束：订单内version.sequence唯一；同一有效请求首次决定唯一；所有命令锁Order并校验expected_revision；确认回执绑定不可变文件摘要。

**测试入口契约（Task 1必须建立）：** `compose.test.yml`含postgres、backend服务；PostgreSQL健康检查通过后backend才开始。backend镜像包含测试依赖、工作目录`/app`挂载`coding/backend`，默认配置`DJANGO_SETTINGS_MODULE=config.settings.test`，连接专用可创建测试库的postgres测试账号；pytest.ini启用pytest-django。运行命令均从`coding`目录执行；数据库卷仅用于测试，命名与生产隔离。

```bash
docker compose -f deploy/compose.test.yml up -d postgres
docker compose -f deploy/compose.test.yml build backend
docker compose -f deploy/compose.test.yml run --rm backend pytest -q
```

当前机器没有Docker／psql／Homebrew；上述命令必须先在执行阶段安装获准的容器环境，或切换已授权的远程PostgreSQL测试库，不可现在宣称能直接运行。若采用远程PG，使用`TEST_DATABASE_URL`连接专用可建测试库账号，在backend目录执行`python3 -m pytest -q`，仍运行同一套测试；绝不回落SQLite。可用bundle Python3.12.14位于`/Users/yuwujian/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3`，Node24.19位于同dependencies的`node/bin/node`；实施阶段将工具目录加入PATH并取得npm，验证版本后再建锁文件。首次Task 1安装/构建以实际执行环境获准网络访问为前提。锁定当时受支持补丁，提交Python与npm锁文件；这里不把框架主版本写成当前最新补丁。前端使用`npm ci`、`npm run typecheck`、`npm run test:e2e`固定脚本；测试命令的执行证据后续填入09阶段，当前全部未执行。

### Task 1: 身份、租户范围与可运行测试入口

**Files:**
- Create: `coding/backend/manage.py`, `coding/backend/pyproject.toml`, `coding/backend/requirements.lock`, `coding/backend/pytest.ini`
- Create: `coding/backend/config/settings/base.py`, `coding/backend/config/settings/test.py`, `coding/backend/config/settings/production.py`, `coding/backend/config/urls.py`, `coding/backend/config/authentication.py`, `coding/backend/config/exceptions.py`
- Create: `coding/backend/identity/models.py`, `coding/backend/identity/api.py`, `coding/backend/identity/policy.py`, `coding/backend/identity/migrations/0001_initial.py`
- Create: `coding/backend/proofing/models.py`, `coding/backend/proofing/api.py`, `coding/backend/proofing/migrations/0001_initial.py`
- Create: `coding/backend/tests/conftest.py`, `coding/backend/tests/test_csrf.py`, `coding/deploy/Dockerfile.backend`, `coding/deploy/compose.test.yml`
- Test: `coding/backend/tests/test_identity.py`

**Interfaces:**
- Produces: `require_member(user: User, tenant_id: UUID, role: str) -> Membership`；`GET /api/session`返回actor、tenant和roles；`POST /api/session/login`使用用户名密码和CSRF，`POST /api/session/logout`撤销会话。
- Produces: `POST /api/orders`建立只含title的草稿；`GET /api/orders`与`GET /api/orders/{id}`返回授权范围。domain草稿允许业务字段空，命令提交时再校验完整性。
- Produces: conftest中`api`、`other_api`两个已登录APIClient及对应tenant；只用于测试fixture的force_login不暴露为产品接口。

- [ ] **Step 0: 建立最小运行与测试壳，证明基础环境可运行。** 仅创建本任务Files中的依赖、manage.py、settings、pytest入口、Dockerfile和compose；声明postgres健康检查、backend测试工作目录与数据库连接。安装获准工具及锁依赖后先执行下面诊断，不实现租户读取与业务命令：

```bash
docker compose -f deploy/compose.test.yml up -d postgres
docker compose -f deploy/compose.test.yml build backend
docker compose -f deploy/compose.test.yml run --rm backend python manage.py check
docker compose -f deploy/compose.test.yml run --rm backend python manage.py shell -c "from django.db import connection; connection.ensure_connection(); assert connection.vendor == 'postgresql'"
```

环境诊断均成功才开始Step 1；不可把缺Docker、镜像构建失败、缺manage.py或无法连接数据库算业务测试红灯。最小测试壳不是绕过后续领域TDD。

**HTTP错误实现契约：** 使用自定义`ApiSessionAuthentication(SessionAuthentication)`，`authenticate_header(request)`返回`Session`，未认证映射401；CSRF失败和已认证越权仍403。`config.exceptions.api_exception_handler`将DRF ValidationError统一转422，将业务Conflict转409，错误结构固定code/message/current_revision；其他框架认证权限异常保持401／403。文件quarantined／rejected不返回内容，409只返回状态及失败原因。下列核心片段属于Task 1明确实现内容：

```python
from rest_framework.authentication import SessionAuthentication
from rest_framework.exceptions import ValidationError
from rest_framework.views import exception_handler

class ApiSessionAuthentication(SessionAuthentication):
    def authenticate_header(self, request):
        return 'Session'

def api_exception_handler(exc, context):
    response = exception_handler(exc, context)
    if response is not None and isinstance(exc, ValidationError):
        response.status_code = 422
    return response
```

补统一错误body和Conflict映射时逐断言测试，不吞500为“成功”；JSONRenderer保持默认即可，`REST_FRAMEWORK`显式指定认证类及exception handler。新增`tests/test_http_contract.py`分别断言匿名401、CSRF403、字段422及修订409。

- [ ] **Step 1: 写失败测试与明确断言。** 新建下列测试；本任务列出的其他用例采用独立测试函数，不能只手工点击。

```python
import pytest
from rest_framework.test import APIClient

@pytest.mark.django_db
def test_unauthenticated_and_cross_tenant(api, other_api):
    assert APIClient().get('/api/session').status_code == 401
    own = api.post('/api/orders', {'title': '本机构记录'}, format='json')
    assert own.status_code == 201
    record_id = own.json()['id']
    assert other_api.get(f'/api/orders/{record_id}').status_code == 403
    assert other_api.get('/api/orders').json()['results'] == []

```

测试fixture具体约定：identity.User继承Django AbstractUser；Tenant使用UUID主键和name；Membership含user、tenant、roles（JSON数组）、is_active。conftest建立两个Tenant、各一User和owner成员，分别force_login；active_tenant记录在服务器session中。增加enforce_csrf_checks=True的独立测试：无CSRF的已登录写请求403；logout后读取401；被禁用Membership之后列表和直接ID访问403。管理员登录使用django-otp的TOTPDevice与StaticDevice恢复码机制：TOTP挑战成功才设服务器已验证状态；恢复码一次性消费，日志不得包含密钥或码，重试限流。[django-otp官方说明](https://django-otp-official.readthedocs.io/en/stable/overview.html)已于2026-09-20核查TOTP与StaticDevice能力；锁版本前核兼容性与许可，未通过第二因素不授予管理写权限。
- [ ] **Step 2: 运行红灯。** 在本idea的`coding`目录执行 `docker compose -f deploy/compose.test.yml run --rm backend pytest tests/test_identity.py -q`。预期当前任务缺失时出现导入失败、404或业务断言失败；数据库不可用／配置错误不是有效红灯，先修运行环境。
- [ ] **Step 3: 实现最小行为。** 在上述Files实现以下关键逻辑，再按接口暴露；每次只覆盖一个失败断言，不顺便引入新业务。

```python
def require_member(user, tenant_id, role):
    membership = Membership.objects.filter(
        user_id=user.id, tenant_id=tenant_id, is_active=True
    ).first()
    if membership is None or role not in membership.roles:
        raise PermissionDenied('当前身份无此操作权限')
    return membership

```

- [ ] **Step 4: 运行绿灯。** 重跑Step 2同一命令，预期全部PASS；再运行 `docker compose -f deploy/compose.test.yml run --rm backend pytest -q`，确保已交付任务没有回归。只有实际日志才可勾选。
- [ ] **Step 5: 独立审查和提交检查点。** 默认按子代理逐任务实现并独立审查，审查覆盖Files及本任务API权限。用户当前未要求git提交，本计划不执行提交；后续明确授权时仅暂存本任务列出的文件再提交，记录提交号或“未提交”。

### Task 2: 命令幂等、修订与多记录草稿

**Files:**
- Create: `coding/backend/common/errors.py`, `coding/backend/common/idempotency.py`, `coding/backend/common/models.py`, `coding/backend/common/migrations/0001_initial.py`
- Create: `coding/backend/proofing/commands.py`, `coding/backend/tests/test_revision_race.py`
- Modify: `coding/backend/proofing/models.py`, `coding/backend/proofing/api.py`, `coding/backend/config/urls.py`
- Test: `coding/backend/tests/test_revision.py`

**Interfaces:**
- Consumes: Task 1会话、tenant范围、草稿创建接口。
- Produces: `PATCH /api/orders/{id}`，请求`title,expected_revision`，响应`id,title,revision`；`CommandReceipt(tenant,actor,operation,object_id,key,request_hash,result_json)`持久化重试结果。
- Produces: `execute_once(scope: tuple, key: str, payload: dict, command: Callable) -> dict`，键冲突409；所有后续命令共用。

- [ ] **Step 1: 写失败测试与明确断言。** 新建下列测试；本任务列出的其他用例采用独立测试函数，不能只手工点击。

```python
import pytest

@pytest.mark.django_db(transaction=True)
def test_stale_edit_keeps_current_record(api):
    first = api.post('/api/orders', {'title': '甲'}, format='json').json()
    url = f"/api/orders/{first['id']}"
    payload = {'title': '修改甲', 'expected_revision': first['revision']}
    r = api.patch(url, payload, format='json', HTTP_IDEMPOTENCY_KEY='edit-1')
    assert r.status_code == 200
    assert api.patch(url, payload, format='json', HTTP_IDEMPOTENCY_KEY='edit-1').json() == r.json()
    assert api.patch(url, {**payload, 'title': '覆盖'}, format='json', HTTP_IDEMPOTENCY_KEY='edit-1').status_code == 409
    assert api.patch(url, payload, format='json', HTTP_IDEMPOTENCY_KEY='edit-2').status_code == 409

```

额外断言：创建两记录，PATCH仅影响指定ID；幂等键同体并发采用两个真实连接、Barrier及事务测试，返回相同result；规范化请求摘要覆盖所有命令字段，数据库唯一约束负责最终竞争仲裁，冲突捕获放在atomic外侧。
- [ ] **Step 2: 运行红灯。** 在本idea的`coding`目录执行 `docker compose -f deploy/compose.test.yml run --rm backend pytest tests/test_revision.py -q`。预期当前任务缺失时出现导入失败、404或业务断言失败；数据库不可用／配置错误不是有效红灯，先修运行环境。
- [ ] **Step 3: 实现最小行为。** 在上述Files实现以下关键逻辑，再按接口暴露；每次只覆盖一个失败断言，不顺便引入新业务。

```python
with transaction.atomic():
    record = Order.objects.select_for_update().get(id=record_id, tenant_id=actor.tenant_id)
    if record.revision != payload['expected_revision']:
        raise Conflict('记录已更新，请重新核对')
    record.title = payload['title']
    record.revision += 1
    record.save(update_fields=['title', 'revision'])

```

- [ ] **Step 4: 运行绿灯。** 重跑Step 2同一命令，预期全部PASS；再运行 `docker compose -f deploy/compose.test.yml run --rm backend pytest -q`，确保已交付任务没有回归。只有实际日志才可勾选。
- [ ] **Step 5: 独立审查和提交检查点。** 默认按子代理逐任务实现并独立审查，审查覆盖Files及本任务API权限。用户当前未要求git提交，本计划不执行提交；后续明确授权时仅暂存本任务列出的文件再提交，记录提交号或“未提交”。

### Task 3: 隔离上传、文件完整性与撤销下载

**Files:**
- Create: `coding/backend/files/models.py`, `coding/backend/files/migrations/0001_initial.py`, `coding/backend/files/storage.py`, `coding/backend/files/validation.py`, `coding/backend/files/api.py`
- Create: `coding/backend/identity/invitations.py`, `coding/backend/tests/test_oss_contract.py`
- Modify: `coding/backend/config/urls.py`, `coding/backend/proofing/models.py`
- Test: `coding/backend/tests/test_files.py`

**Interfaces:**
- Consumes: Task 1服务器身份、Task 2幂等命令。
- Produces: `POST /api/files` multipart(owner_id,file)只写quarantined；`POST /api/files/{id}/validate`校验摘要、大小、类型和扫描结果，返回state=ready或rejected；`GET /api/files/{id}/content`支持Range并每次授权。
- Produces: `PrivateStore.put(key: str, stream: BinaryIO) -> None`, `open(key: str) -> BinaryIO`, `delete(key: str) -> None`；OssPrivateStore用于生产、LocalPrivateStore仅隔离测试。
- Produces: `POST /api/access/{owner_id}/revoke`撤销外部会话；B16/B18邀请token哈希保存，核验码成功后创建限定对象会话；其他项目仅成员访问不开放邀请。

- [ ] **Step 1: 写失败测试与明确断言。** 新建下列测试；本任务列出的其他用例采用独立测试函数，不能只手工点击。

```python
import hashlib
import pytest
from django.core.files.uploadedfile import SimpleUploadedFile

@pytest.mark.django_db
def test_private_upload_and_cross_tenant_denial(api, other_api):
    owner = api.post('/api/orders', {'title': '文件任务'}, format='json').json()
    raw = b'%PDF-1.4\n%%EOF'
    up = api.post('/api/files', {'owner_id': owner['id'], 'file': SimpleUploadedFile('../相同.pdf', raw, content_type='application/pdf')})
    assert up.status_code == 201
    file_id = up.json()['id']
    assert up.json()['state'] == 'quarantined'
    assert other_api.get(f'/api/files/{file_id}/content').status_code == 403
    assert api.get(f'/api/files/{file_id}/content').status_code == 409
    assert up.json()['sha256'] == hashlib.sha256(raw).hexdigest()

```

增加真实格式夹具test-data/valid.pdf、valid.png及恶意/损坏输入；有效文件验证后ready，扩展名伪装、缺对象、摘要不符均rejected。ClamAV或经选定扫描服务执行病毒检查，扫描不可达保留quarantined，不默认通过。保留两个同名文件的不同ID，显示名清洗路径字符。B16/B18外部会话撤销后普通GET和Range都403，过期和错误接收人核验均拒绝；供应商验证码只经开发专用适配器桩测试，生产无配置启动检查失败。OSS集成用环境变量RUN_OSS_CONTRACT=1显式启用，在私有测试桶执行PUT/GET/撤权结果校验；未提供凭据则skip并写“未执行”，不能算绿灯覆盖真实云。
- [ ] **Step 2: 运行红灯。** 在本idea的`coding`目录执行 `docker compose -f deploy/compose.test.yml run --rm backend pytest tests/test_files.py -q`。预期当前任务缺失时出现导入失败、404或业务断言失败；数据库不可用／配置错误不是有效红灯，先修运行环境。
- [ ] **Step 3: 实现最小行为。** 在上述Files实现以下关键逻辑，再按接口暴露；每次只覆盖一个失败断言，不顺便引入新业务。

```python
digest = hashlib.sha256()
size = 0
for chunk in uploaded.chunks():
    size += len(chunk)
    if size > settings.MAX_UPLOAD_BYTES:
        raise UploadTooLarge()
    digest.update(chunk)
# 存储键由服务器UUID产生；此时仅登记quarantined，不可被业务命令引用。

```

- [ ] **Step 4: 运行绿灯。** 重跑Step 2同一命令，预期全部PASS；再运行 `docker compose -f deploy/compose.test.yml run --rm backend pytest -q`，确保已交付任务没有回归。只有实际日志才可勾选。
- [ ] **Step 5: 独立审查和提交检查点。** 默认按子代理逐任务实现并独立审查，审查覆盖Files及本任务API权限。用户当前未要求git提交，本计划不执行提交；后续明确授权时仅暂存本任务列出的文件再提交，记录提交号或“未提交”。

### Task 4: 业务状态机、事务竞争与修订历史

**Files:**
- Modify: `coding/backend/proofing/models.py`, `coding/backend/proofing/commands.py`, `coding/backend/proofing/api.py`, `coding/backend/tests/conftest.py`
- Create: `coding/backend/proofing/migrations/0002_domain_constraints.py`, `coding/backend/tests/test_domain_permissions.py`, `coding/backend/tests/test_domain_concurrency.py`
- Test: `coding/backend/tests/test_stale_confirmation.py`

**Interfaces:**
- Consumes: Task 1 Actor范围、Task 2 expected_revision与幂等、Task 3 ready文件。
- Produces: `POST /api/orders/{id}/commands/{action}`支持publish、decide、revoke、print-review，请求expected_revision和动作字段，返回最新revision和状态。
- Specific contract: ready_file(owner_id)在Task 3 conftest中用真实valid.pdf上传并经本地扫描适配器validate到ready后返回file ID；customer_api(request_id)用Invitation TestCodeProvider固定测试码完成request绑定会话交换，不能用员工会话代客户。新增require_recipient(actor,request)->None，actor的recipient scope必须匹配请求。

- [ ] **Step 1: 写失败测试与明确断言。** 新建下列测试；本任务列出的其他用例采用独立测试函数，不能只手工点击。

```python
import pytest

@pytest.mark.django_db(transaction=True)
def test_stale_confirmation(api, customer_api, ready_file):
    order = api.post('/api/orders', {'title': '校样'}, format='json').json()
    url = f"/api/orders/{order['id']}/commands/publish"
    first = api.post(url, {'asset_id': ready_file(order['id']), 'recipient': 'client@example.test', 'expected_revision': 0}, format='json', HTTP_IDEMPOTENCY_KEY='publish-1').json()
    second = api.post(url, {'asset_id': ready_file(order['id']), 'recipient': 'client@example.test', 'expected_revision': 1}, format='json', HTTP_IDEMPOTENCY_KEY='publish-2')
    assert second.status_code == 200
    response = customer_api(first['request_id']).post(f"/api/requests/{first['request_id']}/decision", {'decision': 'confirmed', 'reason': ''}, format='json', HTTP_IDEMPOTENCY_KEY='decide-1')
    assert response.status_code == 409
    assert api.get(f"/api/orders/{order['id']}").json()['status'] == 'awaiting_confirmation'

```

业务实现顺序：publish建立新请求且旧请求失效；decide核接收人和当前版本，退回原因必填；revoke记录撤销事件；print-review仅当前有效确认可登记；20并发重复确认归为同一决定。 测试`test_domain_permissions.py`分别用owner、员工、外部客户或配送员调用命令，核每条角色边界；`test_domain_concurrency.py`用TransactionTestCase／pytest transaction=True与两个实际PG连接跑竞争，禁止仅顺序请求声称并发已测。每个Review Focus标注Task 4的场景各建独立断言，包括错误后重新读取历史与当前指针。
- [ ] **Step 2: 运行红灯。** 在本idea的`coding`目录执行 `docker compose -f deploy/compose.test.yml run --rm backend pytest tests/test_stale_confirmation.py -q`。预期当前任务缺失时出现导入失败、404或业务断言失败；数据库不可用／配置错误不是有效红灯，先修运行环境。
- [ ] **Step 3: 实现最小行为。** 在上述Files实现以下关键逻辑，再按接口暴露；每次只覆盖一个失败断言，不顺便引入新业务。

```python
with transaction.atomic():
    order = Order.objects.select_for_update().get(id=order_id, tenant_id=actor.tenant_id)
    request = ConfirmationRequest.objects.get(id=request_id, order_id=order.id)
    if request.version_id != order.current_version_id or request.revoked_at:
        raise Conflict('校样已更新或请求已撤销')
    require_recipient(actor, request)
    ConfirmationEvent.objects.create(request=request, decision=decision, actor_id=actor.id)

```

- [ ] **Step 4: 运行绿灯。** 重跑Step 2同一命令，预期全部PASS；再运行 `docker compose -f deploy/compose.test.yml run --rm backend pytest -q`，确保已交付任务没有回归。只有实际日志才可勾选。
- [ ] **Step 5: 独立审查和提交检查点。** 默认按子代理逐任务实现并独立审查，审查覆盖Files及本任务API权限。用户当前未要求git提交，本计划不执行提交；后续明确授权时仅暂存本任务列出的文件再提交，记录提交号或“未提交”。

### Task 5: 数据库任务队列、成果导出与失败恢复

**Files:**
- Create: `coding/backend/jobs/models.py`, `coding/backend/jobs/migrations/0001_initial.py`, `coding/backend/jobs/worker.py`, `coding/backend/jobs/management/commands/run_worker.py`
- Create: `coding/backend/exports/service.py`, `coding/backend/exports/api.py`, `coding/backend/exports/templates/record.html`, `coding/backend/tests/test_worker_recovery.py`
- Modify: `coding/backend/config/urls.py`
- Test: `coding/backend/tests/test_exports.py`

**Interfaces:**
- Consumes: Task 3 PrivateStore与Task 4已冻结业务状态。
- Produces: `POST /api/exports`(owner_id) -> {job_id,state}；`GET /api/exports/{job_id}` -> {state,file_id,error_code}；worker一次事务领取租约，文件处理在事务外，完成时以相同job id幂等发布。
- Produces: `claim_job(worker_id:str, lease_seconds:int)->Job|None`、`run_job(job_id:UUID)->None`、`build_manifest(owner_id:UUID, tenant_id:UUID)->dict`。

- [ ] **Step 1: 写失败测试与明确断言。** 新建下列测试；本任务列出的其他用例采用独立测试函数，不能只手工点击。

```python
import pytest

@pytest.mark.django_db(transaction=True)
def test_export_does_not_leak_other_tenant(api, other_api):
    record = api.post('/api/orders', {'title': '导出任务'}, format='json').json()
    rejected = other_api.post('/api/exports', {'owner_id': record['id']}, format='json')
    assert rejected.status_code == 403
    queued = api.post('/api/exports', {'owner_id': record['id']}, format='json', HTTP_IDEMPOTENCY_KEY='export-a')
    assert queued.status_code == 202
    assert queued.json()['state'] == 'queued'

```

成果内容：全部版本原件＋manifest.json＋WeasyPrint确认PDF，包含核验方式、摘要、决定、撤销和印前复核；PDF.js只做阅读预览。 草稿导出允许导出状态目录，但显著标注“草稿／不完整”，不得声称已确认或完整；丢失应存在原件则失败。补实测：worker领取后崩溃、租约回收、重复执行无双成果；缺原件失败无半成品下载；ZIP逐件重算SHA-256；CSV公式与路径穿越文件名；本项目中文PDF在禁网渲染进程中以长文、20图样本渲染并截图验收，页数／内存／超时有限制。
- [ ] **Step 2: 运行红灯。** 在本idea的`coding`目录执行 `docker compose -f deploy/compose.test.yml run --rm backend pytest tests/test_exports.py -q`。预期当前任务缺失时出现导入失败、404或业务断言失败；数据库不可用／配置错误不是有效红灯，先修运行环境。
- [ ] **Step 3: 实现最小行为。** 在上述Files实现以下关键逻辑，再按接口暴露；每次只覆盖一个失败断言，不顺便引入新业务。

```python
with transaction.atomic():
    job = Job.objects.select_for_update(skip_locked=True).filter(
        state='queued', available_at__lte=timezone.now()
    ).order_by('created_at').first()
    if job is not None:
        job.state = 'running'
        job.lease_owner = worker_id
        job.lease_expires_at = timezone.now() + timedelta(seconds=lease_seconds)
        job.save()
# 独立回收器将过期running租约改回queued；结果对象以job_id固定键幂等发布。

```

- [ ] **Step 4: 运行绿灯。** 重跑Step 2同一命令，预期全部PASS；再运行 `docker compose -f deploy/compose.test.yml run --rm backend pytest -q`，确保已交付任务没有回归。只有实际日志才可勾选。
- [ ] **Step 5: 独立审查和提交检查点。** 默认按子代理逐任务实现并独立审查，审查覆盖Files及本任务API权限。用户当前未要求git提交，本计划不执行提交；后续明确授权时仅暂存本任务列出的文件再提交，记录提交号或“未提交”。

### Task 6: 前端真实闭环、角色入口和多记录

**Files:**
- Create: `coding/frontend/package.json`, `coding/frontend/package-lock.json`, `coding/frontend/tsconfig.json`, `coding/frontend/vite.config.ts`, `coding/frontend/index.html`, `coding/frontend/src/main.ts`
- Create: `coding/frontend/src/api/client.ts`, `coding/frontend/src/auth/session.ts`, `coding/frontend/src/router.ts`, `coding/frontend/src/features/proofing/Workspace.vue`, `coding/frontend/src/features/proofing/RecordDetail.vue`, `coding/frontend/src/features/proofing/History.vue`
- Create: `coding/frontend/playwright.config.ts`, `coding/frontend/e2e/proofing.spec.ts`, `coding/backend/identity/management/commands/seed_e2e.py`

**Interfaces:**
- Consumes: Tasks 1—5真实HTTP接口，`fetch`使用same-origin credentials并发送CSRF；409保留输入并显示最新revision，禁止静默覆盖。
- Produces: `/login`、`/orders`、`/orders/:id`、受限外部入口；按钮仅提示权限，最终以API结果为准。多记录草稿按服务端记录ID保存，退出清理本地敏感输入；加载状态不冒充已提交。

- [ ] **Step 1: 写红灯端到端测试。** seed_e2e只允许DEBUG且显式E2E_TEST_MODE=1，在独立测试库建登录用户和两条标题“第一条”“第二条”的记录；生产配置禁止执行。

```typescript
import { test, expect } from '@playwright/test';
test('记录切换与刷新保留独立历史', async ({ page }) => {
  await page.goto('/login');
  await page.getByLabel('用户名').fill('e2e-owner');
  await page.getByLabel('密码').fill('local-test-only-9');
  await page.getByRole('button', { name: '登录', exact: true }).click();
  await page.getByRole('link', { name: '第一条', exact: true }).click();
  await expect(page.getByRole('heading', { name: '第一条', exact: true })).toBeVisible();
  await page.reload();
  await expect(page.getByRole('heading', { name: '第一条', exact: true })).toBeVisible();
  await page.getByRole('link', { name: '返回列表' }).click();
  await page.getByRole('link', { name: '第二条', exact: true }).click();
  await expect(page.getByRole('heading', { name: '第二条', exact: true })).toBeVisible();
});
```

- [ ] **Step 2: 跑红灯。** 后端测试环境先执行`docker compose -f deploy/compose.test.yml run --rm backend python manage.py seed_e2e`；再执行`docker compose -f deploy/compose.test.yml run --rm --service-ports backend python manage.py runserver 0.0.0.0:8000`启动HTTP；compose的backend需映射127.0.0.1:8000:8000，前端Vite代理http://127.0.0.1:8000。frontend目录运行`npm ci`、`npm run test:e2e -- e2e/proofing.spec.ts`。Playwright webServer启动Vite，`/api`代理测试127.0.0.1:8000，预期页面／动作缺失失败；不是用API假响应让测试通过。
- [ ] **Step 3: 实现页面交互。** 逐页接入真实接口，示例HTTP约定：

```typescript
export async function loadRecord(id: string) {
  const response = await fetch(`/api/orders/${encodeURIComponent(id)}`, { credentials: 'same-origin' });
  if (!response.ok) throw new Error(`读取失败：${response.status}`);
  return response.json();
}
```

工作台落实建单→上传校样→发送确认→客户确认／退回→新发版→当前版印前复核→回执导出；历史显示版本和操作者，上传显示真实已接收／待校验状态。客户入口不暴露员工订单列表，确认与退回均展示正在操作的校样版本。
- [ ] **Step 4: 补失败路径并跑绿灯。** 同一spec增加完整业务动作的实际UI点击：空输入校验、403越权、409冲突保留输入、断网重试、两个记录各自历史。390px与1280px检查水平溢出、键盘操作和下载；`npm run typecheck`及上面E2E命令均PASS后记录截图。B16加PDF.js实际私有网关预览，不把图片示例当PDF上传完成。
- [ ] **Step 5: 子代理审查前端、API集成及Review Focus，再记录检查点。** 当前不执行git提交；不得只测空页面就称主流程完成。

### Task 7: 可部署配置、真实外部服务与恢复验收

**Files:**
- Create: `coding/deploy/compose.production.yml`, `coding/deploy/nginx.conf`, `coding/deploy/.env.example`, `coding/deploy/backup.sh`, `coding/deploy/restore.sh`
- Create: `coding/backend/common/management/commands/check_production.py`, `coding/backend/files/management/commands/verify_objects.py`, `coding/backend/tests/test_deployment_gate.py`
- Create: `coding/README.md`, `coding/deploy/RUNBOOK.md`

**Interfaces:**
- Consumes: 完整应用、PrivateStore、Job和各业务表。`.env.example`仅写键与无效示例：DATABASE_URL、DJANGO_SECRET_KEY、ALLOWED_HOSTS、OSS_REGION、OSS_BUCKET、OSS_ACCESS_KEY_ID、OSS_ACCESS_KEY_SECRET、MAX_UPLOAD_BYTES、CODE_PROVIDER、SCAN_ENDPOINT。
- Produces: `python manage.py check_production`非零拒绝DEBUG、弱密钥、公开桶、测试身份／验证码适配器、未配置扫描器；`python manage.py verify_objects --manifest PATH`核数量与SHA-256，缺失／错误退出非零。

- [ ] **Step 1: 写配置门禁失败测试。**

```python
import pytest
from django.core.management import call_command
from django.core.management.base import CommandError

def test_production_rejects_test_adapter(settings):
    settings.DEBUG = False
    settings.CODE_PROVIDER = 'test'
    with pytest.raises(CommandError):
        call_command('check_production')
```

本项目真实接收人验证码配置不可跳过，测试适配器仅用于独立测试环境。
- [ ] **Step 2: 跑红灯。** coding目录执行`docker compose -f deploy/compose.test.yml run --rm backend pytest tests/test_deployment_gate.py -q`，预期缺失命令或未拒绝测试适配器失败。
- [ ] **Step 3: 实现配置检查与备份脚本。** 生产必需真实RDS/OSS参数；仅本地可用LocalPrivateStore。脚本分别对业务DB做一致性备份、读取原件清单备份对象；恢复到显式`RESTORE_DATABASE_URL`空隔离库和单独对象前缀，拒绝与生产地址相同。实现核查核心：

```python
if settings.DEBUG or settings.PRIVATE_STORE == 'local-test':
    raise CommandError('生产模式禁止测试存储或DEBUG')
```

备份脚本`pg_dump --format=custom`保存快照，恢复用`pg_restore --exit-on-error`；命令凭据从环境注入不打印；对象恢复后执行verify_objects，失败禁止切换。Docker及云账号在实施阶段取得，未安装时本任务不得标通过。
- [ ] **Step 4: 运行最终验证。** 后端全部pytest、前端typecheck与全部E2E通过；真实私有测试桶契约检查单独执行并留证据；用脱敏样本做备份→删隔离副本→恢复→记录数和原件摘要比对、恢复计时。云验证码渠道未获准发测试消息时保留“真实验证未执行”，不掩盖缺口。执行`python manage.py check --deploy`并处理明确告警，检测无生产密钥进入仓库。
- [ ] **Step 5: 独立审查并交付运行包。** RUNBOOK列实际命令、依赖锁版本、迁移顺序、worker重试／未知结果处理、备份恢复、账号初始化和未配置项。交付“已编码／本地已测／真实集成未测／生产未发布”四列状态；只有证据满足才标已完成，不自动开通云资源或部署生产。

## 自检及计划交接

- [ ] R01—R07逐项映射：Task 1身份与草稿；Task 2多记录／修改；Task 3附件／权限；Task 4业务状态及R-A；Task 5成果／历史；Task 6完整交互；Task 7恢复与运行。Task 4约束不会被仅前端按钮禁用替代。
- [ ] 五条Review Focus均进入指定任务测试；真实PG、失败测试、修订号、文件授权和外部服务边界逐项复核。
- [ ] 锁文件和最终补丁在实施取得，不把本文计划代码片段当完整产品；本文件当前没有任何已执行测试结果。
- [ ] 用户审阅计划后，沿用已指定子代理方式分任务实现和审查。每任务交付可独立检验；所有任务通过后仍需整体用户闭环审查。
