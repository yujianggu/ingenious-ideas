# B16｜产品流程与验收

> V3阶段导航：见[全生命周期主计划](00-全生命周期主计划.md)。本文件保留商业参数或执行细则；阶段顺序、产出物与评审按主计划执行，原数字仍为待验证假设。

版本：V2｜本文件是产品或服务生产流程设计，未实现、未测试。

## 1. 客户看到的页面或材料

1. 店员工作台：按待确认、退回、已确认筛单，显著显示当前版本。
2. 订单页：文件预览、版本时间线、指定确认人和撤销入口。
3. 客户页：先显示文件和关键信息，再提供确认及退回原因。
4. 印前页：仅显示当前可用回执，失效记录保持可查。
5. 管理页：员工、额度、导出、退出。

## 2. 最小数据与追溯关系

订单：店铺ID、订单号、标题、客户代号、当前版本ID；版本：不可变ID、原文件名、存储位置、摘要、上传人和时间；请求：版本ID、接收人、期限、撤销时间；事件：请求ID、结果、服务器时间、操作者核验方式；导出清单：订单、全部版本、有效性状态和附件摘要。

所有对象都有固定编号和版本关系；变更能追到谁在何时改了哪项。真实客户数据不默认放入本研究仓库；方案里的例子使用虚构材料，实际试点另设受控位置并在记录中引用。

## 3. 不能省略的业务规则

1. 上传新版本和使旧待确认请求失效必须作为一致操作。
2. 确认提交时再次读取当前版本，不信任页面打开时的状态。
3. 有效确认不能依靠浏览器时间排序。
4. 退回后修改必须创建新版本，不替换原文件。
5. 已确认版本之后有更新时，印前页要求新版本重新确认，不能沿用历史回执。
6. 单纯打开预览不记“已阅读”。

## 4. 场景验收表

以下为未来执行的测试设计。结果当前全部为“未执行”，填写时必须附版本、输入和实际结果，不得仅凭演示通过。

| 编号 | 场景与预期 | 实际结果 |
| --- | --- | --- |
| B16-A01 | 两个员工同时上传：只有一个版本成为当前版本，其余保留历史并提示 | 未执行 |
| B16-A02 | V1页面停留后确认：拒绝并引导查看新请求 | 未执行 |
| B16-A03 | 重复点确认：只留一条有效确认 | 未执行 |
| B16-A04 | 链接撤销后直接访问原附件地址：仍拒绝 | 未执行 |
| B16-A05 | 客户要求撤回确认：记录撤销和原因，印前页立即更新 | 未执行 |
| B16-A06 | 导出后逐项核对摘要：能定位对应原文件。 | 未执行 |

## 5. 交付前后的质量动作

先以虚构样本核验业务规则，再用授权脱敏输入对照旧流程，最后才进行限定范围真实试点。每个用例记录输入版本、操作人、预期、实际、证据位置和问题编号。规则通过不代表客户采用，客户自主完成和付款另看试点文件。

关键结果、原始资料、权限或恢复存在错误时，不让客户继续依赖受影响结果。定位到具体版本和范围，给可靠临时处理方式，修复后重跑相关用例及核心完整流程；保留原问题记录，不把失败覆盖成通过。

## 6. 首版变更控制

实施方案的P0范围为当前基线。新需求登记提出角色、发生频次、替代办法、增加成本、客户是否愿付费；只在真实交付反复遇到后排P1。第二个客户要求不同业务规则时先判断是否仍属同类客户，不能通过无限配置掩盖定制服务。

每次交付带版本说明；服务成果保留修订差异，软件更新保留数据备份和可用的恢复路径。导出、迁移和恢复的验收范围与产品承诺一致，不因文件生成成功就认定内容完整。

[项目目录](../README.md) · [试点排期](04-试点排期与经营复盘.md)
