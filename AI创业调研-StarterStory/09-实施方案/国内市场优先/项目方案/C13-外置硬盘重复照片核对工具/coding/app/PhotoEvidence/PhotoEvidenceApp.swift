import SwiftUI

@main
struct PhotoEvidenceApp: App {
    var body: some Scene {
        WindowGroup {
            Text("照片证据核对")
                .frame(minWidth: 520, minHeight: 320)
        }
    }
}
